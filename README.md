ECS Test

## Bootstrapping

* run `yarn install`
* Install Postgres
* Create database called `ecs_cars`
* Run database migration to create table `yarn db:migrate`

### To Run Tests
`yarn test`

### To Run App
`yarn start`